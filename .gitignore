node_modules/
.next/
out/
build/
.DS_Store
*.pem
npm-debug.log*
.env
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

# backend
backend/.env
backend/.venv/
__pycache__/
*.pyc
.pytest_cache/

# editors
.idea/
.vscode/

# deployment state — holds the database password
deploy/aws/.state

# browser-automation artefacts. Scoped to the repository root rather than
# every directory: the screenshot tool drops stray captures here, but a
# real image asset further down should not be silently skipped.
.playwright-mcp/
/*.png

# project/ and deploy/ used to be ignored wholesale while every file in
# them was tracked, so the rules did nothing except silently drop any NEW
# file added to them. Ignore what should not be committed, by name.